#!/bin/sh
# Schema changes are applied by the container entrypoint on boot
# (scripts/apply-migrations.sh — versioned SQL migrations). Never `prisma db
# push` here: the non-empty production DB shares its instance with the
# unrelated 'epoch' app, and push would try to drop its tables.
echo "Post-deploy: nothing to do (migrations ran in the container entrypoint)."
